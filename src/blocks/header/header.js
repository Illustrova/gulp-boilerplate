import ready from '../../js/utils/documentReady.js';

// eslint-disable-next-line no-console

export default ready(function() {
	const sum = myScripts.sum(3, 3);
	// eslint-disable-next-line no-console
	console.info('The sum of 1 and 2 is:', sum);
});
